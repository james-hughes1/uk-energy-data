/**
 * Placeholder parameters for the dashboard's mock time series, until this is
 * replaced with live data from the backend (e.g. Elexon/BMRS imbalance price,
 * National Grid ESO demand).
 */
export const MOCK_IMBALANCE_PRICE = {
  points: 48,
  baseValue: 65, // approx. typical GB imbalance price, £/MWh
  amplitude: 20,
}
